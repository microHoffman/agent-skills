interface:
  display_name: "GitLab Create MR"
  short_description: "Create GitLab merge requests from local branches"
  default_prompt: "Use $gitlab-create-mr to publish this branch and create a GitLab merge request."
